---
#title: "CH03-CH05"
author: "Fiona Abraham"
date: "2/7/2019"
output: html_document
---

"CHO3 - CH05 Homework Assignment"
"Fiona Abraham"
"STT 3530-101" 
"7 Feburary 2019"


# 3.5.1 Exercises 

#1

```{r}
library(tidyverse)
ggplot(data = mpg) + 
  geom_point(mapping = aes(x = drv, y = cyl)) +
  facet_wrap(~ displ)
```
"Since R will attempt to create a separate facet for every unique value of the continuous variable, the graph will not make sense."



#2 

"In `facet_wrap`, `nrow` and and `ncol` control the numbers of rows and columns, but in `facet_grid` these are implied by the faceting variables."

#3.6.1 Exercises 

#6 

"Plot 1"

```{r}
ggplot(data = mpg, mapping = aes(x = displ, 
y = hwy)) + 
  geom_point() + 
  geom_smooth(se = FALSE)
```

"Plot 2"
```{r}
ggplot(data = mpg, mapping = aes(x = displ, y = hwy)) + 
  geom_smooth(aes(group = drv), se = FALSE) +
  geom_point()
```

"Plot 3"

```{r}
ggplot(data = mpg, mapping = aes(x = displ, y = hwy, color = drv)) + 
  geom_point() + 
  geom_smooth(se = FALSE)
```


"Plot 4"

```{r}
ggplot(data = mpg, mapping = aes(x = displ, y = hwy)) + 
  geom_point(aes(color = drv)) + 
  geom_smooth(se = FALSE)
```  

"Plot 5"

```{r}
ggplot(data = mpg, mapping = aes(x = displ, y = hwy)) + 
  geom_point(aes(color = drv)) +
  geom_smooth(aes(linetype = drv), se = FALSE)
```

"Plot 6"
```{r}
ggplot(data = mpg, mapping = aes(x = displ, y = hwy)) + 
  geom_point(size = 4, colour = "white") + 
  geom_point(aes(colour = drv))
```

#3.9.1 Exercises 

#2 labs() are utilized as labels. These labels make it easier for plots to be accessible to wider audiences

#3 coord_map() is used to project a three-dimensional object onto a two-dimensional plane. It's computationally intensive. coord_quickmap() is less accurate, but it preserves straight lines, making it faster to draw.

#4 

```{r}
ggplot(data = mpg, mapping = aes(x = cty, y = hwy)) +
  geom_point() + 
  geom_abline() +
  coord_fixed()
```


#5.2.4 Exercises 
```{r}
library(nycflights13)
library(tidyverse)
str(flights)

filter(flights, dest == "IAH" | dest == "HOU")
```

```{r}
filter(flights, carrier %in% c("AA","UA","DL"))
```

```{r}
filter(flights, month %in% c(7,8,9))
```

```{r}
filter(flights, arr_delay >= 120 & dep_delay == 0)
```

```{r}
filter(flights, dep_delay >= 60 & dep_delay - arr_delay > 30)
```

```{r}
filter(flights, dep_time %/% 100 %in% c(0:5) | dep_time == 600)
```

#5.3.1 Exercises 

#1 

```{r}
arrange(flights, desc(is.na(dep_time)))
```
#2 

```{r}
arrange(flights, desc(dep_delay))
```

#3 
```{r}
arrange(flights, desc(distance %/% air_time))
```
#4 

```{r}
arrange(flights, desc(air_time))
```

#5.5.2 Exercises 

#1 
```{r}
transmute(flights,
       sched_dep_time = (sched_dep_time %/% 100) * 60 + sched_dep_time %% 100,
       dep_time = (dep_time %/% 100) * 60 + dep_time %% 100)
```
#2 
```{r}
flights2 <- select(flights, air_time, arr_time, dep_time)
mutate(flights2, air_time_new = arr_time - dep_time)
```

"Since dep_time and arr_time are numerical representations of the specific time rather than acutal minutes, they are not the same."

#5.7.1 Exercises 

#2 
```{r}
flights %>%
  group_by(tailnum) %>%
  summarize(prop_on_time = sum(arr_delay <= 30, na.rm = TRUE) / n(),
            mean_arr_delay = mean(arr_delay, na.rm = TRUE),
            flights = n()) %>%
  arrange(prop_on_time, desc(mean_arr_delay))
```  
"The flights arriving within 30 minutes of its specified arrival time is defined"

#3 
```{r}
flights %>%
  group_by(hour) %>%
  summarize(arr_delay = sum(arr_delay > 5, na.rm = TRUE) / n()) %>%
  ggplot(aes(x = hour, y = arr_delay)) +
  geom_col()
```

"Since the evening flights are more likely to get delayed, it would be best to avoid them"








